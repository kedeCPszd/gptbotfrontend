
import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const CustomHeader = () => {
    return (
        <Header
            style={{
                padding: 0,
                background: "white"
            }}
        />
    );
};

export default CustomHeader;
